# Database Connection URL (Neon)
DATABASE_URL="postgresql://user:pass@host/dbname?sslmode=require"

# Server Port
PORT=5000

# Proxy Settings (Optional but recommended for YouTube)
# Format: http://username:password@proxyserver:port
YOUTUBE_PROXY="http://username:password@proxyserver:port"

# SMTP (Email) Settings for Contact/Report forms
# Use Gmail App Passwords if using a Google account
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=465
SMTP_USER="your-gmail@gmail.com"
SMTP_PASS="your-app-password"
CONTACT_EMAIL="your-receiver-email@gmail.com"
